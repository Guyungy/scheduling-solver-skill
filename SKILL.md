---
name: scheduling-solver
description: >
  根据独立的《智能排班规则》记忆、员工数据、排班周期和节假日信息，
  使用“可行性检查 → 休息日求解 → 班次求解 → 合规校验 → 无解预案”的方法生成排班表。
  适用于需要人工维护规则和员工信息、出现规则冲突时需要自动给出妥协方案的企业排班场景。
version: 1.1.0
---

# Scheduling Solver Skill

## 1. Skill 定位

本 Skill 只负责“如何求解排班”。

具体业务规则不写死在 Skill 中。

所有业务规则必须从以下来源读取：

1. 《智能排班规则》记忆
2. 当前员工数据
3. 当前排班周期
4. 员工请假、偏好、人工锁定
5. 节假日与调休信息
6. 用户本轮明确修改的临时规则

如果当前对话中的明确规则与记忆冲突：

当前用户明确指令 > 《智能排班规则》记忆 > Skill 默认求解策略。

---

# 2. 我们已经验证过的求解经验

本 Skill 的默认方法来自一次已经实际跑通的 12 人、30 天、早中晚三班排班。

该次求解并非人工随机拼接。

实际采用了：

- Python
- NumPy
- SciPy `scipy.optimize.milp`
- SciPy sparse matrix
- HiGHS MILP solver

求解被拆成两个主要阶段。

这是本 Skill 最重要的经验：

> 不要一开始就同时求“谁休息 + 谁上什么班 + 所有公平性”。

先解决休息结构，再解决班次结构，模型更稳定，也更容易解释冲突。

---

# 3. 总体流程

每次排班执行：

```text
读取规则记忆
↓
读取员工
↓
过滤有效轮班池
↓
读取周期和节假日
↓
执行容量检查
↓
如果理论无解
    → 生成妥协预案
↓
如果理论可行
    → 阶段A：求休息日
↓
阶段B：求早/中/晚班
↓
阶段C：公平性与软约束优化
↓
完整校验
↓
输出班表
```

---

# 4. 第一步：建立有效轮班池

读取员工后先过滤。

典型过滤：

```text
在职 + 轮班 + participate=true
```

行政班、离职、停用、不参与排班人员不进入普通轮班池。

必须输出：

```text
员工总数
有效轮班人数
行政人数
排除人数
```

不要把“员工总人数”等同于“可轮班人数”。

---

# 5. 第二步：先算数学账

正式建立模型之前，先判断有没有理论解。

设：

```text
D = 排班周期天数

min_day    = 每日早班最低人数
min_middle = 每日中班最低人数
min_night  = 每日晚班最低人数
```

每日最低出勤：

```text
daily_min =
min_day + min_middle + min_night
```

月度最低需求：

```text
required_work_slots =
daily_min × D
```

员工可以提供的出勤：

```text
available_work_slots =
Σ(员工可工作天数)
```

如果：

```text
available_work_slots < required_work_slots
```

直接判断：

```text
当前规则组合理论无解
```

不要继续暴力求解。

---

# 6. 已验证案例中的关键数学判断

在已验证案例中：

```text
有效轮班员工 = 12人
排班周期 = 30天

早班最低 = 5
中班最低 = 1
晚班最低 = 3

每日最低出勤 = 9人
```

因此：

```text
30 × 9 = 270 个工作人次
```

如果所有12人都休8天：

```text
12 × (30 - 8)
=
264 个工作人次
```

缺口：

```text
270 - 264 = 6
```

因此“12人全部休8天”无解。

最终采用的妥协：

```text
6人休8天
6人休7天
```

此时：

```text
6×22 + 6×23
=
270
```

刚好满足每天9人的最低需求。

这类计算必须在求解器之前完成。

---

# 7. 阶段 A：先求休息日

定义二进制变量：

```text
rest[e,d] ∈ {0,1}
```

含义：

```text
员工 e 在日期 d 是否休息
```

已验证案例中每天需要9人工作、12人轮班，因此：

```text
每天必须正好3人休息
```

约束：

```text
Σ_e rest[e,d] = 3
```

月度休息目标：

```text
6名员工：
Σ_d rest[e,d] = 8

另外6名员工：
Σ_d rest[e,d] = 7
```

---

# 8. 休息结构约束

## 最大连续工作6天

如果最大允许连续工作6天，则任意连续7天窗口内至少休1天：

```text
Σ rest[e,d:d+7] >= 1
```

这是一种非常实用的线性化方式。

---

## 避免孤立单日休息

对于内部日期：

```text
rest[e,d] <= rest[e,d-1] + rest[e,d+1]
```

含义：

如果当天休息，前一天或后一天至少还有一天休息。

这样可以减少：

```text
上 → 休 → 上
```

这类单休结构。

注意：

如果业务规则要求“必须至少连续休2天”，可以将其加强为更严格的连续块约束。

---

# 9. 为什么先求休息日

直接同时决定：

```text
休息
早班
中班
晚班
```

会让约束大量耦合。

先确定休息结构后，第二阶段只需要在“工作日”里分配班次。

优点：

1. 更容易找到可行解。
2. 无解时更容易定位原因。
3. 休息公平与班次公平可以分别优化。
4. 人工修改时可以局部重算。
5. 更容易向业务解释。

---

# 10. 阶段 B：分配班次

定义：

```text
x[e,d,s] ∈ {0,1}
```

其中：

```text
s ∈ {day, middle, night}
```

如果该员工当天休息：

```text
Σ_s x[e,d,s] = 0
```

如果当天工作：

```text
Σ_s x[e,d,s] = 1
```

统一表达：

```text
Σ_s x[e,d,s]
=
1 - rest[e,d]
```

---

# 11. 每日班次人数

已验证案例采用最低配置：

```text
早班 = 5人
中班 = 1人
晚班 = 3人
```

因此：

```text
Σ_e x[e,d,day] = 5

Σ_e x[e,d,middle] = 1

Σ_e x[e,d,night] = 3
```

如果业务允许区间：

```text
min_staff[s]
<=
Σ_e x[e,d,s]
<=
max_staff[s]
```

目标人数可作为软目标优化。

---

# 12. 晚班公平

已验证案例中，将每个人的晚班数量限制在：

```text
7 <= Σ_d night[e,d] <= 8
```

最终形成：

```text
部分员工7个晚班
部分员工8个晚班
```

这是比“所有人完全一样”更现实的公平。

如果员工存在晚班偏好，需要从规则记忆读取后修改上下限或目标权重。

---

# 13. 禁止休息后直接进入晚班

如果：

```text
rest[e,d-1] = 1
```

则：

```text
night[e,d] = 0
```

即：

```text
休 → 晚
```

禁止。

这是已验证模型中的实际约束之一。

---

# 14. 避免孤立晚班

核心目标：

不要出现：

```text
早 → 晚 → 早
```

导致某员工只上一天晚班。

已验证模型对内部日期增加了孤立晚班限制。

业务实现时建议用更完整的连续块约束：

当某一天开始进入晚班时：

```text
night[d] = 1
night[d-1] = 0
```

则至少要求：

```text
night[d+1] = 1
```

如果规则要求最小连续晚班N天，则将该逻辑扩展为N天。

---

# 15. 早班连续性

已验证过程中增加了早班连续段约束：

```text
早班一旦形成新的班次段，
尽量连续至少3天。
```

同时限制：

```text
连续早班最多6天。
```

最大6天可以通过任意连续7天窗口：

```text
Σ day[e,d:d+7] <= 6
```

实现。

最小连续块建议采用“班次开始变量”进行标准线性化，生产版本不要只依赖启发式邻接约束。

---

# 16. 中班处理经验

中班人数远少于早班和晚班。

在已验证案例中：

```text
每天只有1个中班
```

因此中班本质是一个稀缺班次。

实际排班中不要先把中班塞满再处理晚班。

更合理的顺序：

```text
先固定休息
→ 确保晚班连续与公平
→ 处理早班连续
→ 中班承担剩余结构调节
```

随后再对中班数量进行公平优化。

---

# 17. 求解器策略

如果 Python 环境具备 SciPy：

推荐：

```python
from scipy.optimize import milp
```

整数变量：

```text
integrality = 1
```

变量边界：

```text
0 <= x <= 1
```

底层默认可使用 HiGHS 求解 MILP。

已验证案例实际返回：

```text
Optimization terminated successfully.
HiGHS Status: Optimal
```

---

# 18. 为什么当前 Skill 不强制 OR-Tools

CP-SAT 非常适合复杂排班，生产系统也可以使用。

但已验证班表实际跑通时使用的是：

```text
SciPy MILP + HiGHS
```

因此本 Skill 记录真实验证经验，不虚构成 OR-Tools。

如果未来规则加入大量：

- 连续块
- 布尔逻辑
- 条件约束
- 排班序列
- 多目标权重

可以考虑迁移到：

```text
Google OR-Tools CP-SAT
```

---

# 19. 无解诊断

求解器返回 infeasible 时不要马上降级规则。

先区分：

## 类型 A：容量无解

例如总工作人次不足。

这种情况在求解前就应该发现。

## 类型 B：结构无解

总人次足够，但连续休息、班次衔接、晚班连续等组合产生冲突。

## 类型 C：个人约束无解

例如：

大量员工同一天请假。

## 类型 D：人工锁定无解

例如：

人工锁定导致某天早班人数无法达到最低要求。

---

# 20. 生成妥协预案

无解后至少生成3个预案。

每个预案必须包含：

```text
调整规则
调整幅度
释放多少资源
解决多少缺口
影响对象
风险
```

默认逻辑：

## 预案一

降低低优先级约束。

例如：

```text
6人休8天
6人休7天
```

并记录下月补休。

## 预案二

增加资源。

例如行政支援、机动岗、跨组支援。

## 预案三

降低业务覆盖。

例如低峰日临时减少最低人数。

P0方案默认最后推荐，并要求人工确认。

---

# 21. 最终校验

MILP 返回 Optimal 不代表业务规则一定全部正确。

必须另外执行程序化校验。

逐日检查：

```text
早班人数
中班人数
晚班人数
休息人数
```

逐人检查：

```text
总出勤
总休息
晚班数量
中班数量
最大连续工作
连续休息
孤立晚班
休→晚
人工锁定
请假冲突
```

输出违规列表。

只有：

```text
solver feasible
+
business validation passed
```

才能标记：

```text
可发布
```

---

# 22. 人工修改策略

人工修改以后优先固定原有解。

例如用户要求：

```text
张三 9/10 改早班
```

不要重新随机生成整个月。

应：

```text
锁定新要求
+
尽量锁定未受影响的原排班
+
只释放相关员工与相邻日期
+
重新求解局部窗口
```

建议局部窗口：

```text
目标日期前后3~7天
```

因为连续工作、休息、晚班规则通常会影响相邻日期。

---

# 23. 排班结果必须解释

最终输出：

## 班表

```text
日期 | 早班 | 中班 | 晚班 | 休息
```

## 员工统计

```text
员工 | 早 | 中 | 晚 | 休 | 总出勤
```

## 求解摘要

```text
有效轮班人数
总工作人次
总休息人次
采用的妥协方案
```

## 合规报告

```text
P0
P1
P2
P3
P4
Soft
```

---

# 24. 真实案例中的最终结果经验

已验证案例最终形成：

```text
12人
30天
每天5早 + 1中 + 3晚 + 3休
```

员工休息：

```text
6人休8天
6人休7天
```

总工作人次：

```text
270
```

晚班：

```text
每人7~8天
```

这证明：

```text
容量检查
+
休息MILP
+
班次MILP
+
结果校验
```

这条路线可以跑通该规模的排班问题。

---

# 25. 禁止做法

禁止：

1. 用自然语言直接“编”完整月班表而不校验。
2. 没算总人次就开始排。
3. 求解器无解后随机删规则。
4. 把低优先级公平规则放在P0前面。
5. 只看每日人数，不检查个人连续性。
6. 只看个人天数，不检查每日覆盖。
7. 为满足“每人一样”制造业务违规。
8. 把求解器返回 Optimal 等同于业务100%合规。
9. 人工微调一次就推翻整个已有班表。
10. 声称使用了实际没有使用的求解器。

---

# 26. 推荐生产升级路线

当前已验证方案：

```text
SciPy MILP + HiGHS
```

适合：

```text
中小规模
规则相对稳定
线性约束较多
```

如果未来扩展为：

```text
几十到几百员工
几十种班次
复杂技能覆盖
大量条件逻辑
多部门
跨月
动态订单预测
```

建议升级为：

```text
OR-Tools CP-SAT
```

但仍保留本 Skill 的核心架构：

```text
先容量检查
→ 再求可行解
→ 再优化
→ 再解释
→ 再校验
```
