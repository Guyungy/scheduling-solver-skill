---
name: scheduling-solver
description: >
  企业智能排班求解 Skill。读取独立的《智能排班规则》记忆、员工数据、排班周期、节假日、请假、偏好和人工锁定，
  先进行容量与可行性检查，再使用 Google OR-Tools CP-SAT 生成排班；若规则组合无解，则定位冲突并生成多个妥协预案。
version: 2.0.0
---

# Scheduling Solver Skill

## 1. 定位

本 Skill 负责“怎么求解排班”，不负责永久保存具体业务规则。

业务规则来源：

1. 《智能排班规则》记忆
2. 当前员工数据
3. 当前排班周期
4. 请假、偏好、人工锁定
5. 节假日和调休
6. 用户本轮明确修改

优先级：

当前用户明确指令 > 排班规则记忆 > Skill 默认求解策略。

如果规则记忆中不存在某条业务要求，不要自行创造。

---

## 2. 默认求解器

主求解器：

```text
Google OR-Tools CP-SAT
```

Python：

```python
from ortools.sat.python import cp_model
```

历史验证方案：

```text
SciPy MILP + HiGHS
```

MILP 保留为参考和备用方案，不再作为本 Skill 的默认生产求解器。

切换原因：企业排班包含大量布尔逻辑、条件约束、连续班次和离散组合，CP-SAT 对这些规则的表达和后续维护更直接。

---

## 3. 已验证结果

2026 年 9 月验证案例：

```text
有效轮班员工：12
排班天数：30
每天：5早 + 1中 + 3晚 + 3休
```

容量检查：

```text
最低工作需求 = 30 × 9 = 270 人次
```

若 12 人全部休 8 天：

```text
12 × 22 = 264 人次
```

缺口：

```text
6 人次
```

采用最低影响妥协：

```text
6 人休 8 天
6 人休 7 天
```

CP-SAT 回归测试实际结果：

```text
STATUS: OPTIMAL
pytest: 1 passed
```

该模型同时验证：

- 每天 5 早班
- 每天 1 中班
- 每天 3 晚班
- 每天 3 休息
- 6 人休 8 天
- 6 人休 7 天
- 每人晚班 7~8 天
- 连续工作不超过 6 天
- 禁止“休息 → 晚班”
- 避免孤立晚班
- 避免孤立单休
- 中班数量尽量均衡

---

## 4. 总体执行流程

```text
读取规则记忆
↓
读取员工数据
↓
过滤有效轮班池
↓
读取排班周期、节假日、请假、偏好和人工锁定
↓
容量检查
↓
结构性可行性检查
↓
如果理论无解
    → 量化缺口
    → 定位冲突规则
    → 生成预案一/二/三
↓
如果理论可行
    → 建立 CP-SAT 模型
    → 先找可行解
    → 再优化软约束
↓
完整业务校验
↓
输出班表和合规报告
```

禁止跳过容量检查直接让大模型编班表。

---

## 5. 有效排班池

先过滤员工。

典型条件：

```text
在职
AND 参与轮班
AND participate_in_scheduling = true
```

默认排除：

- 行政班
- 离职
- 停用
- 明确不参与排班

不得因为人数不足自动把行政人员加入轮班池。

必须先输出：

```text
员工总数
有效轮班人数
行政人数
排除人数
临时支援人数
```

---

## 6. 容量检查

设：

```text
D = 排班周期天数
minimum_staff_per_day = 所有班次最低人数之和
```

最低工作需求：

```text
required_work_slots = D × minimum_staff_per_day
```

最大可提供工作量：

```text
available_work_slots = Σ 每名员工最大可工作天数
```

若：

```text
available_work_slots < required_work_slots
```

直接判断：

```text
当前规则组合理论无解
```

不要浪费求解器时间，也不要伪造班表。

---

## 7. CP-SAT 核心变量

推荐统一使用：

```text
x[e,d,s] ∈ {0,1}
```

含义：

```text
员工 e 在日期 d 是否处于状态 s
```

状态包括：

```text
day
middle
night
rest
```

每人每天只能有一个状态：

```python
model.add_exactly_one(x[e,d,s] for s in shifts)
```

---

## 8. 每日班次覆盖

读取规则记忆中的最低、目标和最高人数。

硬约束：

```text
min_staff[s] <= Σ_e x[e,d,s] <= max_staff[s]
```

如果业务要求固定人数，则直接使用等式。

P0 最低业务人数默认不得由智能体自动放宽。

---

## 9. 员工个人约束

支持：

- 请假
- 不可排日期
- 固定休息
- 允许班次
- 禁止班次
- 人工指定班次
- 人工锁定
- 硬性员工诉求
- 软偏好

人工锁定示例：

```python
model.add(x[e,d,'day'] == 1)
```

用户人工锁定导致无解时，不得擅自解除，应把它列入冲突来源。

---

## 10. 连续工作

最大连续工作 N 天，可以使用滑动窗口：

```text
任意 N+1 天窗口内至少休 1 天
```

例如最大连续工作 6 天：

```python
model.add(sum(rest[e,d] for d in window_7_days) >= 1)
```

最小连续工作、最小连续某班次等规则，优先使用“开始变量 + OnlyEnforceIf”建模。

---

## 11. 班次衔接

例如禁止：

```text
休息 → 晚班
```

可以表达为：

```python
model.add(rest[e,d-1] + night[e,d] <= 1)
```

对于更复杂的条件：

```text
如果 A，则 B
```

优先使用：

```python
model.add(...).only_enforce_if(condition)
```

这是 CP-SAT 相比传统 MILP 更适合本项目的重要原因之一。

---

## 12. 连续晚班

如果规则要求晚班至少连续 N 天：

建立：

```text
night_start[e,d]
```

当某天从非晚班进入晚班时，将其视为晚班段开始。

若开始，则强制后续 N 天满足晚班要求。

简单规则“禁止孤立晚班”可以约束：

```text
当日是晚班时，前后至少有一天也是晚班
```

边界日期需结合上月/下月状态处理；如果没有跨月数据，应明确标记边界假设。

---

## 13. 连续休息

最小连续休息、最大连续休息应从规则记忆读取。

对于“避免孤立单休”，可以使用邻接约束。

如果连续休息规则与 P0 业务覆盖冲突，可根据规则优先级将其列入妥协候选，不得静默破坏。

---

## 14. 公平性

公平性通常作为软目标，不追求所有人绝对一致。

优化对象可以包括：

- 总工作天数
- 晚班数量
- 中班数量
- 周末值班
- 节假日值班
- 员工偏好满足率
- 欠休补偿

CP-SAT 可以建立最大值、最小值和偏差变量，再最小化团队差距。

例如：

```python
model.add_max_equality(max_mid, middle_counts)
model.add_min_equality(min_mid, middle_counts)
model.minimize(max_mid - min_mid)
```

---

## 15. 求解策略

优先分两层思考：

第一层：可行性。

```text
能不能满足所有硬约束？
```

第二层：优化。

```text
在可行解中，哪一张班表更公平、更符合偏好？
```

不要一开始为了追求完美公平而让模型难以找到可行解。

CP-SAT 参数建议：

```python
solver.parameters.max_time_in_seconds = 30
solver.parameters.num_search_workers = 8
```

大型模型可按业务时限调整。

---

## 16. 无解诊断

无解分为：

### A. 容量无解

总工作人次不足。

### B. 结构无解

总人数够，但连续休息、连续工作、班次衔接等组合冲突。

### C. 个人约束无解

例如同一天大量请假或大量员工禁止某班次。

### D. 人工锁定无解

人工指定导致班次无法覆盖。

### E. 人员结构无解

人数够，但主管、技能、新老搭配等角色资源不足。

---

## 17. 无解预案

出现无解时禁止只回复“排不出来”。

至少生成三个预案。

每个预案必须包含：

```text
调整哪条规则
规则优先级
调整幅度
释放多少资源
影响哪些员工/日期
业务风险
是否需要后续补偿
```

默认排序：

```text
预案一：放宽低优先级软约束/P3/P4
预案二：增加支援资源
预案三：调整 P0 业务覆盖，仅人工确认后允许
```

---

## 18. 已验证无解处理案例

12 人、30 天、每天最低 9 人工作。

如果每人休 8 天：

```text
最大可工作 264 人次
最低需求 270 人次
缺口 6 人次
```

推荐妥协：

```text
6 人休 8 天
6 人休 7 天
```

同时记录 6 名员工各欠休 1 天，下月优先补偿。

---

## 19. 人工修改

用户修改已有班表时：

1. 将修改视为人工锁定。
2. 尽量固定原班表。
3. 只释放相关员工和相邻日期。
4. 使用 CP-SAT 做局部重新求解。
5. 再次校验所有受影响规则。

推荐局部窗口：

```text
目标日期前后 3~7 天
```

不得因为修改一天就随机重排整个月。

---

## 20. 强制校验

求解器返回 `OPTIMAL` 或 `FEASIBLE` 后仍必须进行业务校验。

逐日检查：

- 各班次人数
- 休息人数
- 技能和角色覆盖

逐人检查：

- 请假冲突
- 禁止班次
- 人工锁定
- 连续工作
- 连续休息
- 连续晚班
- 休息后晚班
- 月度工作/休息
- 晚班数量
- 节假日值班

只有：

```text
solver feasible
AND
business validation passed
```

才能标记为：

```text
可发布
```

---

## 21. 输出

班表：

```text
日期 | 星期 | 节假日 | 早班 | 中班 | 晚班 | 休息
```

员工汇总：

```text
员工 | 早班 | 中班 | 晚班 | 休息 | 总出勤 | 节假日值班 | 异常
```

同时输出：

- 有效轮班人数
- 最低工作需求
- 实际总工作人次
- 求解状态
- 使用的妥协方案
- 规则违规项
- 公平性指标

---

## 22. 工具职责边界

大模型负责：

```text
理解自然语言规则
→ 结构化规则
→ 判断需要哪些约束
→ 调用求解器
→ 解释结果
→ 生成妥协预案
```

CP-SAT 负责：

```text
真正搜索组合
→ 验证硬约束可行性
→ 优化软目标
```

不要让语言模型直接凭感觉编完整月班表。

---

## 23. MILP 备用策略

SciPy MILP + HiGHS 仍然适合：

- 规则高度线性
- 重点是成本、产能、工时或资源配比
- 连续与条件逻辑较少

如果运行环境没有 OR-Tools，但具备 SciPy，可以使用 MILP 作为备用求解器。

备用不代表与 CP-SAT 同时求解；应明确记录当前实际调用的求解器。

---

## 24. 禁止行为

禁止：

1. 不做容量检查直接排班。
2. 让大模型随机生成月班表并声称已校验。
3. 偷偷违反硬约束。
4. 无解时随机删除规则。
5. 自动降低 P0。
6. 忽略请假或人工锁定。
7. 自动征用行政人员。
8. 为绝对公平破坏高优先级业务规则。
9. 把求解器返回 `OPTIMAL` 等同于业务规则全部正确。
10. 声称使用了实际没有调用的求解器。

---

## 25. 当前推荐技术路线

```text
智能体
↓
读取《智能排班规则》记忆
↓
规则结构化
↓
容量检查
↓
OR-Tools CP-SAT
↓
可行 / 无解
↓
业务校验
↓
班表 / 冲突预案
```

当前版本默认采用 CP-SAT。
